#include<stdio.h>

int main() {
    int sum,p,a;
    sum=0;
    scanf("%d",&a);
    scanf("%d",&p);
    while(a>=2){
        sum=sum+a;
        a=a-(a*p)/100;
        }
        sum=sum+a;
        printf("%d",sum);
        return 0;
}
